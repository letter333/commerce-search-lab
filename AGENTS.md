# commerce-search-lab 작업 지침

## 기준 문서

- 제품 범위·검색 원칙: `docs/search-project-plan.md`.
- 현재 단계·다음 작업·검증 기록: `docs/worklog.md`. 구현 작업을 시작하거나 인계할 때 확인하고, 완료 후 실제 결과로 갱신한다.
- 실행·완료 기준: `docs/harness.md`. 검색 API나 평가를 바꾸면 `docs/evaluation-contract.md`도 확인한다.
- 개발 시드: `docs/catalog-50.json`, `docs/search-scenarios-30.json`. 기대값은 사람이 검토할 초안이며 검색 실행 결과가 아니다.

## 프로젝트 스킬

- 기능 구현·버그 수정·리팩터링을 시작하거나 범위가 커지면 `.agents/skills/commerce-search-small-steps/SKILL.md`로 검증 가능한 단위를 정한다.
- 실제 작업 착수, 커밋·푸시·PR 전달과 머지 후 정리에는 `.agents/skills/commerce-search-git-delivery/SKILL.md`로 작업 이슈·단일 PR·변경 범위·검증·전송 대상을 확인한다. 스킬·지침·계획 작성이나 점검 요청 자체가 원격 이슈/PR 생성·커밋·푸시·머지 지시는 아니다.
- 검색 평가 실행·결과 해석·모드 비교에는 `.agents/skills/commerce-search-evaluate/SKILL.md`를 사용한다.
- 상품·검색 시나리오·기대 ID 변경과 시드 계약 실패 진단에는 `.agents/skills/commerce-search-seeds/SKILL.md`를 사용한다.
- 해당 작업에 필요한 스킬만 읽는다. 프로젝트 검증에는 기존 Gradle 하네스를 재사용한다.

## 작업 방식

- 한국어로 작업 결과를 설명한다. 기능 추가·버그 수정·동작 변경은 작은 단위의 **TDD**로 진행한다. 테스트 작성 → 의도한 실패 확인(Red) → 최소 구현 → 통과 확인(Green) → 리팩터링 순서를 따른다.
- 구현 코드보다 동작을 검증하는 테스트를 먼저 작성한다. 버그는 수정 전에 재현 테스트를 실행한다. 환경·의존성 오류를 Red로 세지 않고, 실제 관찰한 실패 이유와 통과 명령을 기록한다. 상세 절차와 순수 리팩터링·문서 변경의 적용 범위는 `docs/harness.md`의 TDD 기준을 따른다.
- 사용자 변경과 이미 staged된 파일을 보존한다. 관련 없는 리팩터링·버전 업그레이드를 섞지 않는다.
- 실제 작업 시작 전에 해당 작업 ID의 이슈를 생성하거나 기존 이슈를 재사용한다. 작업 ID 하나·이슈 하나·PR 최대 하나를 연결한다. 닫히거나 머지된 PR까지 확인해 중복 생성을 막고, 실제 PR 머지 후 이슈 종료를 확인한다. 상세 절차와 권한 범위는 Git 전달 스킬을 따른다.
- 작업 커밋은 Conventional Commits의 `type(scope): 한국어 요약`과 `Refs #실제이슈번호`를 사용한다. scope는 적절한 변경 영역이 없으면 생략할 수 있다. 한 PR에 여러 작은 검증 완료 커밋을 허용하고, 이슈 종료 키워드는 PR 본문에 둔다. 타입·호환성 변경·이력 처리의 상세 규칙은 Git 전달 스킬을 따른다.
- 기존 Java 21 / Spring Boot / Gradle Wrapper를 사용한다. Elasticsearch·Nori·공식 Java Client 버전은 M1에서 호환성을 확인하고 결정 근거와 함께 고정한다.
- 제품 코드의 기본 패키지는 `org.letter33.commercesearchlab`이다. 테스트용 평가 도구는 `src/test`에 두어 배포 앱에 포함하지 않는다.
- 테스트가 실패하면 구현·테스트·기획 중 어느 계약이 잘못됐는지 조사한다. 통과만을 위해 기대값·분모·필터를 약화하지 않는다.
- 코드 변경은 `./gradlew check`(Windows: `./gradlew.bat check`)로 검증한다. 데이터만 변경하면 `verifySeeds`를 먼저 사용한다. 문서만 바꾸면 링크와 명령을 확인한다.
- 검증 실패·미실행·환경 제약은 통과와 구분해 기록한다. 실제로 실행하지 않은 검색 품질이나 성능을 주장하지 않는다.

## 민감정보와 Git

- 실제 환경변수 파일, 비밀번호·토큰·자격증명, 개인키·키 저장소, 민감한 원문 로그를 Git에 포함하지 않는다. 파일 이름과 관계없이 코드·문서·예시·평가 결과에도 실제 비밀값을 쓰지 않는다.
- 비밀값은 실행 환경에서 주입한다. 공유 설정은 환경변수 참조를 사용하고, 루트 `.env.example`에는 빈 값이나 명확한 placeholder만 둔다. 실제 `.env`·로컬 Spring 설정·개인용 `gradle.properties`는 `.gitignore` 대상으로 유지한다.
- 파일을 추가하기 전에 `git check-ignore`로 제외 여부를 확인한다. 민감 파일을 `git add -f`로 강제 추가하지 않는다. 커밋 전에는 작업 트리와 staged 내용, 푸시 전에는 전송할 전체 이력을 확인한다. 검사 중 비밀값을 출력하거나 보고서에 복사하지 않는다.
- `.gitignore`는 이미 추적된 파일이나 과거 이력에서 비밀정보를 제거하지 않는다. 발견 시 커밋·푸시를 중단하고 값 없이 경로와 상태를 보고한 뒤, 로컬 원본 보존·추적 해제·필요한 이력 정리와 자격증명 폐기/교체를 범위에 맞게 처리한다.
- JSONL 원문 로그는 로컬에 둔다. `docs/results/<실험 ID>/runs.jsonl`은 평가 증거 보존을 위한 제외 규칙의 예외일 뿐이며, 다른 결과 파일과 함께 민감정보가 없는지 검토한 뒤에만 포함한다.

## 검색에서 지킬 계약

- 입력 원문을 보존한다. 별칭·정규화와 오타 제안을 구분하고, 오타 제안은 명시적으로 선택한 경우에만 재검색한다.
- 정상 브랜드·SKU를 보호한다. `MONOLIT`, `SW-001`, `sw-001`을 오타 교정 대상으로 취급하지 않는다.
- `baseline → query_v1 → ranking_v1` 비교는 데이터·분석기·필터를 동일하게 유지한다. 분석기 변경은 별도 실험 버전이다.
- 제안 선택 후 결과는 원문 점수에 섞지 않는다. 정답 없는 질의는 Hit@5·MRR에서 제외하고 별도로 보고한다. 제안이 없을 때 제안 정확도는 N/A다.
- 검색 엔진 오류·시간 초과를 0건 검색으로 반환하지 않는다. 가격·색상·카테고리 필터를 자동 완화하지 않는다.
- 상품 ID를 안정적인 문서 ID로 사용한다. Bulk 항목별 실패를 확인하고 새 인덱스 검증 후 alias를 전환한다.
- 데이터나 기대값을 변경하면 snapshot/version과 판정 근거를 함께 검토한다. 동일 `intent_group`을 개발/최종 평가셋에 나누지 않는다.
- 가상 상품 50개·개발 질의 30개의 수치를 실제 커머스 성능이나 최종 평가 성적으로 일반화하지 않는다.

## 범위

현재 범위는 의류 상의 검색과 비교 실험이다. 결제·회원·Kafka·CDC·Kubernetes·벡터 검색을 선행 구현하지 않는다. 실제 검색 결과가 나온 뒤 실패 유형에 근거해 후속 실험을 선택한다.
